# Copia este archivo a .env (git lo ignora) y completa los valores:
#   cp .env.example .env

DB_PASSWORD=tpinge3

# Solo si corres npm run dev FUERA de Docker:
DATABASE_URL=postgresql://tpinge3:tpinge3@localhost:5432/tpinge3_db
